# Telegram-бот для аналитики по видео

Telegram-бот, который отвечает на вопросы о видео-аналитике на естественном языке, используя локальную LLM модель для генерации SQL-запросов.

## 🚀 Как это работает

### Архитектура

```
Пользователь → Telegram → Бот → LLM → SQL → PostgreSQL → Результат → Пользователь
```

1. **Пользователь задает вопрос** на русском языке в Telegram
   - Например: "Сколько всего видео есть в системе?"

2. **Бот получает сообщение** и передает его в `SQLGenerator`

3. **LLM модель генерирует SQL запрос**:
   - Модель получает промпт с описанием схемы БД
   - Промпт содержит правила генерации SQL
   - Модель преобразует естественный язык в SQL
   - Результат: `SELECT COUNT(*) FROM videos`

4. **SQL выполняется в PostgreSQL**:
   - Запрос отправляется в базу данных
   - Получается числовой результат

5. **Результат возвращается пользователю**:
   - Бот отправляет число обратно в Telegram

### Технологии

- **Python 3.8+** - основной язык программирования
- **aiogram 3.x** - фреймворк для Telegram-ботов (асинхронный)
- **PostgreSQL** - реляционная база данных
- **asyncpg** - асинхронный драйвер для PostgreSQL
- **transformers** - библиотека для работы с LLM моделями
- **PyTorch** - фреймворк для машинного обучения
- **python-dotenv** - загрузка переменных окружения из .env

### Структура проекта

```
tg_bot/
├── bot.py              # Основной файл бота (обработка сообщений)
├── sql_generator.py    # Генерация SQL через LLM
├── database.py         # Работа с PostgreSQL
├── load_data.py        # Загрузка данных из JSON в БД
├── check_db.py         # Проверка подключения к БД
├── requirements.txt    # Зависимости Python
├── .env               # Конфигурация (БД, токен бота)
├── model.safetensors  # Локальная LLM модель
└── videos.json        # Данные для загрузки
```

## 📋 Установка и настройка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройте базу данных PostgreSQL

Создайте базу данных и пользователя:

```sql
CREATE DATABASE tg_bot;
CREATE USER postgres WITH PASSWORD 'Kohkau11999';
GRANT ALL PRIVILEGES ON DATABASE tg_bot TO postgres;
```

### 3. Создайте файл `.env`

```bash
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Kohkau11999
POSTGRES_DB=tg_bot
DB_HOST=localhost
DB_PORT=5432
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 4. Получите токен Telegram-бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен и вставьте в `.env`

### 5. Подготовьте LLM модель

Поместите файлы модели в директорию проекта:
- `model.safetensors` - веса модели
- `config.json` - конфигурация модели
- Файлы токенизатора (если есть)

Если модель не загружена, бот будет использовать простой fallback.

## 🎯 Команды для запуска

### Шаг 1: Загрузите данные в базу данных

```bash
python load_data.py videos.json
```

Эта команда:
- Создает таблицы `videos` и `video_snapshots`
- Загружает данные из JSON файла
- Выводит количество загруженных видео

### Шаг 2: Проверьте подключение к БД (опционально)

```bash
python check_db.py
```

Проверяет:
- Подключение к PostgreSQL
- Наличие таблиц
- Количество записей

### Шаг 3: Запустите бота

```bash
python bot.py
```

Бот начнет работать и будет отвечать на сообщения в Telegram.

## 💬 Примеры использования

После запуска бота отправьте ему вопросы на русском языке:

- **"Сколько всего видео есть в системе?"**
  - Ответ: число (например, `358`)

- **"Сколько видео у креатора с id aca1061a9d324ecf8c3fa2bb32d7be63?"**
  - Ответ: количество видео этого креатора

- **"Сколько видео набрало больше 100000 просмотров?"**
  - Ответ: количество видео с просмотрами > 100000

- **"На сколько просмотров в сумме выросли все видео 28 ноября 2025?"**
  - Ответ: сумма прироста просмотров за указанную дату

- **"Сколько разных видео получали новые просмотры 27 ноября 2025?"**
  - Ответ: количество уникальных видео с новыми просмотрами

## 🔧 Как работает генерация SQL

### Промпт для LLM

Модель получает промпт с:
1. **Схемой базы данных** - описание таблиц и полей
2. **Правилами генерации** - как строить SQL запросы
3. **Запросом пользователя** - вопрос на русском языке
4. **Информацией о датах** - если в запросе есть даты, они преобразуются в формат SQL

### Пример промпта

```
Database schema:
videos: id(VARCHAR), creator_id(VARCHAR), video_created_at(TIMESTAMP), ...
video_snapshots: video_id(VARCHAR), delta_views_count(INTEGER), ...

Rules:
- Use COUNT(*) from videos to count videos
- Use SUM(delta_*) from video_snapshots for increments
...

Query: Сколько всего видео есть в системе?

Generate PostgreSQL SQL returning one number:

SQL:
```

### Обработка ответа модели

1. Модель генерирует SQL запрос
2. Код очищает ответ от markdown форматирования (```sql)
3. Извлекается только SQL запрос (до точки с запятой или новой строки)
4. Проверяется, что запрос начинается с SELECT
5. Запрос выполняется в БД

## 🐛 Решение проблем

### Модель не загружается

Если видите сообщение "Model not found. Using fallback":
- Убедитесь, что `config.json` находится в директории проекта
- Проверьте наличие файлов токенизатора
- Бот будет работать, но с ограниченной функциональностью

### Ошибка подключения к БД

- Проверьте, что PostgreSQL запущен
- Убедитесь, что данные в `.env` правильные
- Проверьте права доступа пользователя к базе данных

### Бот не отвечает

- Проверьте токен в `.env`
- Убедитесь, что бот запущен (`python bot.py`)
- Проверьте логи на наличие ошибок

## 📝 Лицензия

Проект создан для тестового задания.
