# Telegram-бот для аналитики по видео

Telegram-бот, который отвечает на вопросы о видео-аналитике на естественном языке, используя локальную LLM модель для генерации SQL-запросов.

## ⚡ Быстрый старт

1. **Создайте `.env`** с настройками БД и токеном бота (см. раздел "Запуск проекта")
2. **Запустите через Docker:**
   ```bash
   docker-compose up -d
   ```
3. **Готово!** Данные загружаются автоматически, бот работает и отвечает на вопросы в Telegram

📖 **Подробные инструкции:**
- [Загрузка данных](LOAD_DATA.md) - как загрузить JSON файл в БД
- [Docker команды](DOCKER.md) - краткая справка по Docker

## 🏗️ Архитектура системы

### Общая схема

```
Пользователь → Telegram → Бот (aiogram) → SQLGenerator (LLM) → SQL → PostgreSQL → Результат → Пользователь
```

### Компоненты

1. **Telegram Bot (bot.py)** - принимает сообщения от пользователей через Telegram API
2. **SQL Generator (sql_generator.py)** - преобразует естественный язык в SQL запросы с помощью LLM
3. **Database (database.py)** - управляет подключением к PostgreSQL и выполнением запросов
4. **LLM Model** - локальная модель `suriya7/t5-base-text-to-sql` для генерации SQL

## 🔄 Подход к преобразованию текстовых запросов в SQL

### Стратегия преобразования

Система использует **двухуровневый подход**:

1. **Основной метод**: LLM модель генерирует SQL на основе схемы БД и запроса пользователя
2. **Fallback метод**: Правила на основе ключевых слов для типовых запросов

### Процесс генерации SQL

```
Запрос пользователя (русский язык)
    ↓
Промпт для LLM (схема БД + запрос)
    ↓
LLM генерирует SQL
    ↓
Очистка и валидация SQL
    ↓
Выполнение в PostgreSQL
    ↓
Возврат числового результата
```

### Описание схемы данных для LLM

Модель получает **полную схему БД в формате CREATE TABLE**:

```sql
CREATE TABLE videos (
    id VARCHAR(255) PRIMARY KEY,
    creator_id VARCHAR(255) NOT NULL,
    video_created_at TIMESTAMP NOT NULL,
    views_count INTEGER NOT NULL DEFAULT 0,
    likes_count INTEGER NOT NULL DEFAULT 0,
    comments_count INTEGER NOT NULL DEFAULT 0,
    reports_count INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE video_snapshots (
    id BIGSERIAL PRIMARY KEY,
    video_id VARCHAR(255) NOT NULL REFERENCES videos(id) ON DELETE CASCADE,
    views_count INTEGER NOT NULL DEFAULT 0,
    likes_count INTEGER NOT NULL DEFAULT 0,
    comments_count INTEGER NOT NULL DEFAULT 0,
    reports_count INTEGER NOT NULL DEFAULT 0,
    delta_views_count INTEGER NOT NULL DEFAULT 0,
    delta_likes_count INTEGER NOT NULL DEFAULT 0,
    delta_comments_count INTEGER NOT NULL DEFAULT 0,
    delta_reports_count INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Почему именно CREATE TABLE?**
- Модель обучена на SQL схемах в этом формате
- CREATE TABLE содержит всю необходимую информацию: имена таблиц, колонок, типы данных, связи
- Модель может самостоятельно понять структуру и связи между таблицами

### Промпт для LLM

Используется **минималистичный промпт** без дополнительных правил и примеров:

```
translate to SQL: {запрос пользователя}

{схема БД в формате CREATE TABLE}
```

**Пример промпта:**

```
translate to SQL: Сколько всего видео есть в системе?

CREATE TABLE videos (
    id VARCHAR(255) PRIMARY KEY,
    creator_id VARCHAR(255) NOT NULL,
    ...
);

CREATE TABLE video_snapshots (
    ...
);
```

**Почему такой подход?**
- Модель `suriya7/t5-base-text-to-sql` специально обучена для преобразования текста в SQL
- Она понимает структуру БД из CREATE TABLE без дополнительных инструкций
- Минимальный промпт снижает риск галлюцинаций и ошибок

### Используемая LLM модель

**Модель**: `suriya7/t5-base-text-to-sql`

- **Тип**: T5 (Text-to-Text Transfer Transformer) - encoder-decoder архитектура
- **Назначение**: Специализирована на преобразовании естественного языка в SQL запросы
- **Формат**: Локальная модель в формате `safetensors`
- **Загрузка**: Автоматически загружается из папки `model/` при запуске

### Обработка ответа модели

После генерации SQL выполняется:

1. **Очистка от форматирования** - удаление markdown (```sql), префиксов
2. **Исправление ошибок** - замена неправильных имен таблиц (например, `table_video_snapshots` → `video_snapshots`)
3. **Валидация** - проверка, что SQL начинается с SELECT и содержит корректные имена таблиц
4. **Fallback** - если SQL невалиден, используется правило на основе ключевых слов

### Fallback механизм

Для типовых запросов используется простой парсинг:

- "сколько всего видео" → `SELECT COUNT(*) FROM videos`
- "выросли просмотры" + дата → `SELECT SUM(delta_views_count) FROM video_snapshots WHERE DATE(created_at) = ...`
- "креатора с id" → `SELECT COUNT(*) FROM videos WHERE creator_id = ...`

## 🚀 Запуск проекта

### Вариант 1: Запуск через Docker (рекомендуется)

#### Шаг 1: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# База данных
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Kohkau11999
POSTGRES_DB=tg_bot
DB_PORT=5432

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id
API_ID=your_api_id
API_HASH=your_api_hash
```

#### Шаг 2: Получение токена Telegram-бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. После создания бота BotFather предоставит токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
5. Скопируйте токен и вставьте в `.env` в поле `TELEGRAM_BOT_TOKEN`

**Дополнительно (опционально):**
- `TELEGRAM_CHAT_ID` - ваш Telegram ID (можно получить у [@userinfobot](https://t.me/userinfobot))
- `API_ID` и `API_HASH` - для работы с Telegram API (если требуется)

#### Шаг 3: Подготовка модели

Убедитесь, что папка `model/` содержит файлы модели `suriya7/t5-base-text-to-sql`:

```
model/
├── config.json
├── model.safetensors
├── tokenizer.json
├── tokenizer_config.json
└── другие файлы токенизатора
```

**Как скачать модель:**
```bash
# Используя huggingface-cli
huggingface-cli download suriya7/t5-base-text-to-sql --local-dir model/

# Или вручную с HuggingFace Hub
```

#### Шаг 4: Запуск через Docker Compose

```bash
# Сборка и запуск всех сервисов (PostgreSQL + Bot)
docker-compose up -d

# Просмотр логов бота
docker-compose logs -f bot

# Просмотр логов PostgreSQL
docker-compose logs -f postgres
```

**Данные загружаются автоматически!** При первом запуске контейнера:
- Система ждет готовности PostgreSQL
- Проверяет, есть ли данные в БД
- Если БД пустая, автоматически загружает данные из `videos.json`
- Затем запускает бота

**Отключение автоматической загрузки:**

Добавьте в `.env`:
```bash
AUTO_LOAD_DATA=false
```

**Ручная загрузка данных (если нужно):**

```bash
# Загрузить данные в контейнер
docker-compose exec bot python load_data.py videos.json
```

Подробная инструкция по загрузке данных: [LOAD_DATA.md](LOAD_DATA.md)

### Вариант 2: Локальная установка (без Docker)

#### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Шаг 2: Настройка PostgreSQL

Создайте базу данных и пользователя:

```sql
CREATE DATABASE tg_bot;
CREATE USER postgres WITH PASSWORD 'Kohkau11999';
GRANT ALL PRIVILEGES ON DATABASE tg_bot TO postgres;
```

#### Шаг 3: Создание файла `.env`

```bash
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Kohkau11999
POSTGRES_DB=tg_bot
DB_HOST=localhost
DB_PORT=5432
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id
API_ID=your_api_id
API_HASH=your_api_hash
```

#### Шаг 4: Получение токена бота

См. инструкцию выше в разделе "Вариант 1: Docker"

#### Шаг 5: Подготовка модели

Поместите файлы модели в директорию `model/` (см. инструкцию выше)

#### Шаг 6: Загрузка данных

```bash
python load_data.py videos.json
```

#### Шаг 7: Запуск бота

```bash
python bot.py
```

## 📥 Загрузка данных

Подробная инструкция по загрузке JSON-файла в базу данных: [LOAD_DATA.md](LOAD_DATA.md)

**Быстрая команда:**

```bash
# Локально
python load_data.py videos.json

# В Docker
docker-compose exec bot python load_data.py videos.json
```

## 💬 Примеры использования

После запуска бота отправьте ему вопросы на русском языке:

- **"Сколько всего видео есть в системе?"**
  - Ответ: число (например, `358`)

- **"Сколько видео у креатора с id aca1061a9d324ecf8c3fa2bb32d7be63?"**
  - Ответ: количество видео этого креатора

- **"Сколько видео набрало больше 100000 просмотров?"**
  - Ответ: количество видео с просмотрами > 100000

- **"На сколько просмотров в сумме выросли все видео 28 ноября 2025?"**
  - Ответ: сумма прироста просмотров за указанную дату

- **"Сколько разных видео получали новые просмотры 27 ноября 2025?"**
  - Ответ: количество уникальных видео с новыми просмотрами

## 🐳 Docker команды

### Основные команды

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Остановка и удаление контейнеров
docker-compose down

# Остановка с удалением данных БД
docker-compose down -v

# Пересборка образа бота
docker-compose build bot

# Просмотр логов
docker-compose logs -f bot
```

### Выполнение команд в контейнере

```bash
# Проверить состояние БД
docker-compose exec bot python check_db.py

# Загрузить данные
docker-compose exec bot python load_data.py videos.json
```

## 🛠️ Технологии

- **Python 3.11+** - основной язык программирования
- **aiogram 3.x** - асинхронный фреймворк для Telegram-ботов
- **PostgreSQL 15** - реляционная база данных
- **asyncpg** - асинхронный драйвер для PostgreSQL
- **transformers** - библиотека для работы с LLM моделями
- **PyTorch** - фреймворк для машинного обучения
- **python-dotenv** - загрузка переменных окружения
- **Docker & Docker Compose** - контейнеризация и оркестрация

## 📁 Структура проекта

```
tg_bot/
├── bot.py                  # Основной файл бота (обработка сообщений)
├── sql_generator.py        # Генерация SQL через LLM
├── database.py             # Работа с PostgreSQL
├── load_data.py            # Загрузка данных из JSON в БД
├── check_db.py             # Проверка подключения к БД
├── requirements.txt        # Зависимости Python
├── Dockerfile              # Образ для Docker
├── docker-compose.yml      # Конфигурация Docker Compose
├── docker-entrypoint.sh    # Скрипт запуска в Docker
├── .env                    # Конфигурация (БД, токен бота)
├── .dockerignore           # Исключения для Docker
├── model/                  # Локальная LLM модель
│   ├── config.json
│   ├── model.safetensors
│   └── tokenizer files
├── videos.json             # Данные для загрузки
├── README.md               # Основная документация
├── LOAD_DATA.md            # Инструкция по загрузке данных
└── DOCKER.md               # Краткая инструкция по Docker
```

## 🐛 Решение проблем

### Модель не загружается

Если видите сообщение "Model not found. Using fallback":
- Убедитесь, что папка `model/` содержит `config.json` и `model.safetensors`
- Проверьте наличие файлов токенизатора
- Бот будет работать с ограниченной функциональностью (fallback)

### Ошибка подключения к БД

**В Docker:**
- Проверьте, что PostgreSQL контейнер запущен: `docker-compose ps`
- Убедитесь, что данные в `.env` правильные
- Проверьте логи: `docker-compose logs postgres`

**Локально:**
- Проверьте, что PostgreSQL запущен
- Убедитесь, что данные в `.env` правильные
- Проверьте права доступа пользователя к базе данных

### Бот не отвечает

- Проверьте токен в `.env`
- Убедитесь, что бот запущен: `docker-compose ps` (Docker) или проверьте процесс (локально)
- Проверьте логи: `docker-compose logs bot` (Docker) или вывод консоли (локально)
- Убедитесь, что нет конфликта с другим экземпляром бота

### Проблемы с памятью

Если модель большая, может потребоваться увеличить память:
- **Docker Desktop**: Settings → Resources → Memory (рекомендуется минимум 4GB)
- **Локально**: Убедитесь, что достаточно RAM для работы с PyTorch

### Ошибки при загрузке данных

См. подробную инструкцию в [LOAD_DATA.md](LOAD_DATA.md)

## 📝 Лицензия

Проект создан для тестового задания.
