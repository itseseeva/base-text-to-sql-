# 📥 Инструкция по загрузке данных

## Загрузка JSON-файла в базу данных

### Вариант 1: Локальная загрузка

1. **Убедитесь, что PostgreSQL запущен и настроен**

2. **Проверьте файл `.env`** содержит правильные данные подключения:
```bash
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Kohkau11999
POSTGRES_DB=tg_bot
DB_HOST=localhost
DB_PORT=5432
```

3. **Запустите скрипт загрузки:**
```bash
python load_data.py videos.json
```

Скрипт автоматически:
- Подключается к базе данных
- Создает таблицы `videos` и `video_snapshots` (если их нет)
- Очищает существующие данные (TRUNCATE)
- Загружает данные из JSON файла
- Выводит количество загруженных видео

### Вариант 2: Загрузка через Docker

**Автоматическая загрузка (по умолчанию):**

Данные загружаются автоматически при первом запуске контейнера! Просто запустите:

```bash
docker-compose up -d
```

Система автоматически:
- Ждет готовности PostgreSQL
- Проверяет наличие данных в БД
- Если БД пустая, загружает данные из `videos.json`
- Запускает бота

**Отключение автоматической загрузки:**

Добавьте в `.env`:
```bash
AUTO_LOAD_DATA=false
```

**Ручная загрузка данных:**

Если нужно загрузить данные вручную:

1. **Убедитесь, что контейнеры запущены:**
```bash
docker-compose up -d
```

2. **Загрузите данные:**
```bash
docker-compose exec bot python load_data.py videos.json
```

### Что делает скрипт `load_data.py`

1. **Подключение к БД** - использует настройки из `.env`
2. **Создание таблиц** - вызывает `db.create_tables()`
3. **Очистка данных** - удаляет старые данные через TRUNCATE
4. **Парсинг JSON** - читает файл и извлекает массив видео
5. **Вставка видео** - для каждого видео:
   - Вставляет запись в таблицу `videos`
   - Использует `ON CONFLICT` для обновления существующих записей
6. **Вставка снапшотов** - для каждого снапшота:
   - Вставляет запись в таблицу `video_snapshots`
   - Связывает с видео через `video_id`

### Формат JSON файла

Скрипт поддерживает два формата:

**Вариант 1: Массив видео**
```json
[
  {
    "id": "video-id-1",
    "creator_id": "creator-id-1",
    "video_created_at": "2025-11-01T10:00:00Z",
    "views_count": 1000,
    "likes_count": 50,
    "comments_count": 10,
    "reports_count": 0,
    "snapshots": [...]
  }
]
```

**Вариант 2: Объект с ключом `videos`**
```json
{
  "videos": [
    {
      "id": "video-id-1",
      ...
    }
  ]
}
```

### Проверка загрузки

После загрузки проверьте данные:

**Локально:**
```bash
python check_db.py
```

**В Docker:**
```bash
docker-compose exec bot python check_db.py
```

### Пример вывода

```
Found videos: 358
Loaded 358 videos
Tables created
```

### Возможные ошибки

1. **FileNotFoundError** - файл `videos.json` не найден
   - Решение: убедитесь, что файл находится в корне проекта

2. **Ошибка подключения к БД** - PostgreSQL не запущен или неправильные настройки
   - Решение: проверьте `.env` и убедитесь, что PostgreSQL запущен

3. **Ошибка парсинга JSON** - неверный формат файла
   - Решение: проверьте формат JSON файла

4. **Ошибка вставки данных** - проблемы с типами данных
   - Решение: проверьте формат дат и типы полей в JSON

